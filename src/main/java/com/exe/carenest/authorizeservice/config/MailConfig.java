package com.exe.carenest.authorizeservice.config;

import org.springframework.context.annotation.Configuration;

/**
 * Mail configuration is no longer needed as we're using Resend API for email sending.
 * The Resend API configuration is handled via application.yml properties.
 */
@Configuration
public class MailConfig {
}